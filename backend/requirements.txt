# Dependencias de producción (backend API + worker).
# Los prototipos sueltos (demo.py, ingest.py, api.py) siguen corriendo solo con la
# librería estándar; estas son para el stack en Docker.
fastapi>=0.115
uvicorn[standard]>=0.30
celery>=5.4
redis>=5.0
httpx>=0.27
psycopg[binary]>=3.2
# Para ingesta de mapas (shapefile -> GeoJSON) se necesitaría además:
#   geopandas  shapely   (hacen la imagen bastante más pesada; se añaden cuando toque)
