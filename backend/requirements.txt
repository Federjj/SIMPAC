# Dependencias de producción (backend API + worker).
# El prototipo (backend/prototipo/) y las pruebas (backend/tests/) corren solo con la
# librería estándar; estas son para el stack en Docker.
# Las del cargador de mapas FEN están aparte en requirements-mapas.txt.
fastapi>=0.115
uvicorn[standard]>=0.30
celery>=5.4
redis>=5.0
httpx>=0.27
psycopg[binary]>=3.2
pypdf>=5.1                 # texto de los comunicados ENFEN (PDF), solo lo usa el worker
